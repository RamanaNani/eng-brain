__pycache__/
*.pyc
.DS_Store

# gbrain page mirrors — regenerated by `gbrain capture`, and they duplicate docs/.
# Left on disk for the sync, kept out of git so captured_at doesn't churn history.
/analysis/
/projects/

# Strix playbooks fetched by pentest/sync-upstream.sh — not vendored, not ours to commit.
/skills/pentest/upstream/
/skills/pentest/.upstream-tag
/skills/pentest/.last-check

# Partially-recovered originals: provenance only, not runnable. install.sh
# strips them for clone users; gitignore keeps them off the plugin channel too.
/skills/_eng-brain/bin/_recovered/
